info:
  name: Ricerca nome
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:9080/search/oggetto
  body:
    type: json
    data: |-
      {
        "repo": "2e903a00-dd58-47b6-8370-bfa28df1409a",
        "query": [
          {
            "tipo": "schema",
            "schema": "accessorio",
            "campo": "descrizione",
            "criteria": {"text": "dd", "operator": "equals", "case": true}
          }
        ],
        "pageIndex": 0
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
