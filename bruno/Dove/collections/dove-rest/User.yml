info:
  name: User
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:9080/user
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
