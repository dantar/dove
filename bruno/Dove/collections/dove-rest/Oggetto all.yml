info:
  name: Oggetto all
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:9080/oggetto/list
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
