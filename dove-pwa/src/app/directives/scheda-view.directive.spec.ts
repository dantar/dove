import { SchedaViewDirective } from './scheda-view.directive';

describe('SchedaViewDirective', () => {
  it('should create an instance', () => {
    const directive = new SchedaViewDirective();
    expect(directive).toBeTruthy();
  });
});
