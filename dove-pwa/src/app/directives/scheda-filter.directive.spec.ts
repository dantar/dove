import { SchedaFilterDirective } from './scheda-filter.directive';

describe('SchedaFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new SchedaFilterDirective();
    expect(directive).toBeTruthy();
  });
});
